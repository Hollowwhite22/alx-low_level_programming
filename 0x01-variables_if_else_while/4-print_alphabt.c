#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print all lowecase alphabets except q and e
 * Return: always 0
 */

int main(void)
{
	char = 'a';

	for (char = 'a'; char <= 'z'; char++)
	{
		putchar(char);
	}
	putchar('\n');
	Reurn(0);
}
